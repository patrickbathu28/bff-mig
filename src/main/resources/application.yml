spring:
  application:
    name: bff-mig
  data:
    mongodb:
      host: localhost
      port: 27017
      database: bff-mig
    res:
      defaultMediaType: application/json

server:
  port: 9090

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

swagger:
  version: 1.0
  description : APIs BFF